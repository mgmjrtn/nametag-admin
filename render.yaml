services:
  - type: web
    name: nametag-admin
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: APPS_SCRIPT_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
